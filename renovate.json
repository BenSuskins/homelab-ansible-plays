{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended"
  ],
  "packageRules": [
    {
      "matchDatasources": [
        "docker"
      ],
      "matchManagers": [
        "ansible"
      ],
      "pinDigests": true,
      "groupName": "Ansible Docker Images"
    }
  ],
  "ansible": {
    "fileMatch": ["tasks/docker/**/*.yml"]
  },
  "hostRules": [
    {
      "hostType": "docker",
      "username": "{{env:MEND_DOCKER_USERNAME}}",
      "password": "{{env:MEND_DOCKER_TOKEN}}"
    }
  ]
}